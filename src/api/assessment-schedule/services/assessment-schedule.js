'use strict';

/**
 * assessment-schedule service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::assessment-schedule.assessment-schedule');
