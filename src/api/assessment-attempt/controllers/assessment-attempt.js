'use strict';

/**
 * assessment-attempt controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::assessment-attempt.assessment-attempt');
