'use strict';

/**
 * assessment-attempt router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::assessment-attempt.assessment-attempt');
